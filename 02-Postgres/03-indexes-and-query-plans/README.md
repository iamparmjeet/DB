# Indexes and query plans

Experiments that explain how PostgreSQL selects sequential, index, or bitmap scans. Capture the query, `EXPLAIN (ANALYZE, BUFFERS)` output, environment, and interpretation for each lab.

## Labs

- [`01-real-indexing-demo`](./01-real-indexing-demo/README.md) — selective email lookup versus broad status predicate.
